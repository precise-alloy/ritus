# tasks/ — Execution task files

See `docs/PROJECT_CONTEXT.md` section `## Team conventions` for the task directory layout.

Created by `ticket-review` skill. Executed by `execute-task` skill.
Never modified by workflow setup or upgrades — always preserved.
