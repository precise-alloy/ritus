# Project Context — {{PROJECT_NAME}}

> Initially generated by the `setup` skill from `docs/profiles/*.yml`.
> Edit `docs/profiles/*.yml`, then re-run the `setup` skill to regenerate this file as the project evolves.

## Identity

| Field     | Value                  |
|-----------|------------------------|
| Project   | `{{PROJECT_NAME}}`     |
| Language  | `{{PRIMARY_LANGUAGE}}` |
| Framework | `{{FRAMEWORK}}`        |
| Team size | `{{TEAM_SIZE}}`        |

## Source layout

| Area               | Paths                          |
|--------------------|--------------------------------|
| Business logic     | `{{BUSINESS_LOGIC_PATHS}}`     |
| Web/API            | `{{WEB_API_PATHS}}`            |
| Models/contracts   | `{{MODEL_CONTRACT_PATHS}}`     |
| Integrations       | `{{INTEGRATION_PATHS}}`        |
| Frontend/UI        | `{{FRONTEND_PATHS}}`           |
| Generated/ignored  | `{{GENERATED_ARTIFACT_PATHS}}` |

## Build commands

| Command             | What      |
|---------------------|-----------|
| `{{BUILD_CMD}}`     | Build     |
| `{{TYPECHECK_CMD}}` | Typecheck |
| `{{TEST_CMD}}`      | Test      |
| `{{LINT_CMD}}`      | Lint      |

## Authentication

Auth pattern: `{{AUTH_PATTERN}}`

- Token verification: `{{AUTH_TOKEN_VERIFICATION}}`
- Tenant/workspace scoping: `{{AUTH_TENANT_SCOPING}}`
- Role/permission model: `{{AUTH_ROLE_MODEL}}`

## Error handling

{{ERROR_HANDLING_PATTERN}}

## Testing

{{TESTING_PATTERN}}

Test locations:

{{TEST_LOCATION_CONVENTIONS}}

## Team conventions

| Field              | Value                         |
|--------------------|-------------------------------|
| Git platform       | `{{GIT_PLATFORM}}`            |
| Git workflow       | `{{GIT_FLOW}}`                |
| Workflow owner     | `{{WORKFLOW_OWNER}}`          |
| Ticket format      | `{{TICKET_FORMAT}}`           |
| QA mode            | `{{QA_MODE}}`                 |
| EPIC memory expiry | `{{MEMORY_EXPIRY_DAYS}}` days |

Branch format:

{{BRANCH_FORMAT}}

Tasks path convention:

{{TASKS_PATH_CONVENTION}}

PR reviewers: `{{PR_REVIEWERS}}`
Default base branch: `{{DEFAULT_BASE_BRANCH}}`
Traceability policy: `{{TRACEABILITY_POLICY}}`

## Documentation layout

| Purpose        | Path                          |
|----------------|-------------------------------|
| Ticket reviews | `{{TICKET_REVIEW_DOCS_PATH}}` |
| Architecture   | `docs/ARCHITECTURE.md`        |
| Decisions      | `docs/DECISIONS.md`           |
| Lessons        | `docs/LESSONS.md`             |
| Module docs    | `docs/modules/`               |

## Requirement source precedence

1. Explicit user instruction in the current conversation
2. Latest authoritative ticket comments or discussion notes
3. Ticket acceptance criteria
4. Ticket description
5. Existing review/task files

## AI tools

`{{AI_TOOLS}}`

## Model routing

{{MODEL_ROUTING}}

## Module map

{{MODULE_MAP}}

## Project-specific constraints

{{PROJECT_CONSTRAINTS}}
